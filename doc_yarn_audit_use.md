# Yarn audit 使用

Yarn 项目可以用 audit 命令检查依赖漏洞，适合老前端项目和 monorepo。

## 常用命令

```bash
yarn audit
```

```bash
yarn audit --json
```

```bash
yarn npm audit
```

```bash
yarn npm audit --severity high
```

```bash
yarn why lodash
```

不同 Yarn 版本命令差异较大，记录时写清项目使用 Yarn classic 还是 Berry。
