# LSE 常用用法

LSE（Linux Smart Enumeration）适合 Linux 本地枚举。常用参数：不同颜色输出、详细模式和摘要模式。

```bash
./lse.sh
```

```bash
./lse.sh -l1
```

```bash
./lse.sh -l2
```

```bash
./lse.sh -h
```

小记录：LSE 的结构化输出适合当作补充清单。和 LinEnum、linPEAS 一起看时，能少漏一些基础线索。
