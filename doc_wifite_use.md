# Wifite 使用

Wifite 用于自动化无线测试流程，适合实验环境快速验证。

## 常用命令

```bash
sudo wifite
```

```bash
sudo wifite --kill
```

```bash
sudo wifite --dict wordlist.txt
```

```bash
sudo wifite --bssid AA:BB:CC:DD:EE:FF
```

```bash
sudo wifite --pmkid
```

自动化工具方便但噪声大，正式记录里要写清它实际调用了哪些底层工具。
